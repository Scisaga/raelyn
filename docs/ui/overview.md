# UI 设计总览

本文记录当前已经实现的 UI 结构与主要交互，而不是早期草图。

项目当前配色主题重载见 [UI 主题配色](theme.md)。

## 技术形态

- 运行时：FastAPI 提供静态 SPA。
- 前端源码：`ui/`
- 构建产物：`static/`
- 技术栈：TailwindCSS + Alpine.js
- 资源策略：不依赖 CDN，样式、脚本、PWA 资源都从本地静态目录加载。

## 当前页面结构

### 启动门面

- 应用启动时先显示全屏“事件语义星域”门面。底层 WebGL canvas 使用 ready 事件地图生成的匿名真实数据样本：只保留量化三维坐标、事件时间、语义色、成员权重和随机相位，不包含标题、实体、canonical ID 或来源信息。
- 样本在固定语义坐标中按事件时间做 24 秒循环显隐，并仅使用缓慢镜头视差；事件点不会为了装饰而改变相对位置。桌面最多绘制 6,000 点，窄屏降为 1,800 点。
- 中央品牌图形使用非对称黑洞吸积盘和事件视界，不再使用玻璃球及外围同心圆；`RAELYN / 事件语义星域` 作为固定品牌层级，“输入访问令牌”只保留为表单提示。
- 启动阶段文案继续叠在 canvas 上，启动阶段仍会依次探测系统状态、资源分发策略和概览首屏资源。
- 若主站 API 开启 Bearer Token，会在同一层启动门面中显示 token 输入框，不切换到独立页面。
- 登录前不能访问受 Bearer Token 保护的事件地图接口，因此动画读取构建时复制到 `/static/data/startup-event-field.bin` 的公开匿名样本，不在浏览器中请求私有标题或事件详情。样本可用以下命令从指定 ready 快照重新生成：

  ```bash
  .venv/bin/python scripts/dev/generate-startup-starfield.py \
    --playlist-id PLAYLIST_ID \
    --limit 6000 \
    --start-date 2008-05-24 \
    --end-date 2026-07-25
  ```

- 当系统启用减少动态效果时，星域停在固定时间切片，吸积盘和加载点等 CSS 动画停用；WebGL 或匿名样本加载失败时回退到静态深色渐变背景。

### 侧边导航

当前导航项：

- 概览
- 媒体
- 视频
- 播放列表
- 任务
- MCP Server
- 设置

说明：

- 侧栏品牌入口、浏览器 favicon 和 PWA 主屏幕图标统一使用“R 事件视界”标识：原品牌带衬线与叶片缺口的 R 保持大尺寸主体占比和黄绿—青色渐变；吸积盘沿用启动门面的遮挡关系，但只保留一张低饱和雾蓝—灰紫色倾斜椭圆盘和同盘的前缘，不与 R 争用青绿色相，也不叠加第二描边或独立高光。
- 品牌源文件位于 `ui/assets/brand/`；侧栏和 PWA 使用标准事件视界版本，浏览器 favicon 单独使用小尺寸光学校正版：R 与吸积盘不缩放，只把蓝黑底板扩展至画布边缘、提高底板和外框对比，并同时提供 SVG 与 32px PNG。构建脚本分别从两个版本的 512px 主 PNG 派生 favicon、Apple Touch 与 PWA 尺寸，运行时不依赖外部资源。
- `playlist` 是隐藏的详情页路由，不直接出现在侧边栏。
- 页面基于 History API 切换，非 `/api` 与 `/static` 路径会回退到 SPA 壳。

## 当前页面能力

### 概览页

- 展示媒体数、视频数、待处理任务、失败任务等统计。
- 概览页的大额调用数与 token 数使用 `K/M/G/T` 等紧凑单位展示，避免服务状态卡片横向溢出。
- 展示最近媒体、最近视频、热门播放列表卡片。
- 播放列表卡片会带最近简报摘要片段。

### 媒体页

- 列表展示媒体头像、名称、provider、监控开关、本地视频数。
- 支持新增媒体、导入媒体、导出媒体。
- 新增媒体后默认未启用监控。
- 支持“同步近期视频”和“同步全部历史视频”。
- 支持开启 / 关闭监控。
- 自动停用的媒体会显示原因标签，例如 YouTube 频道不存在时显示“来源不可用”。
- 直接进入媒体页时，首屏只请求当前列表页数据；向下滚动到列表底部后继续按页加载更多媒体。媒体选择器所需的轻量媒体索引延后到创建播放列表、视频筛选等交互入口加载。
- 当资源分发已回退为 `/api/assets` 代理模式时，媒体列表请求不携带 S3 预签名 URL，头像通过代理内容地址加载，减少首屏服务端签名开销。

### 视频页

- 支持按媒体、状态、关键词、发布时间范围筛选。
- 列表展示封面、媒体头像、发布时间、状态、下载产物状态。
- 提供视频播放器弹窗。
- 在播放器内可查看 transcript、播放资产、重新转写、生成笔记。

### 播放列表列表页

- 展示播放列表概览卡片。
- 支持创建播放列表。
- 支持从概览页、媒体页等入口跳转到详情页。
- 直接进入播放列表列表页时，页面数据加载完成后再静默刷新 `/api/stats`，避免概览统计阻塞播放列表首屏展示。
- 播放列表创建 / 编辑使用 `/api/media/options` 加载轻量媒体选项，不在首屏请求完整媒体列表。

### 播放列表详情页

主视图：

- 顶部展示头像、背景图、标题、描述、媒体预览、视频数与时间范围。
- 中部包含视频列表、播放器；左侧栏在事件审核与当前周期总结之间切换。
- 事件审核默认按当前时间轴周期请求事件列表与状态计数，周期口径基于事件 `available_at`，避免把长期预测目标时间误读为当前期。
- 事件审核使用单列审核卡片流：卡片默认展示日期、状态、类型、置信度、摘要与实体，点击后在卡片内展开方向、幅度、超预期、影响链和证据；实体过滤框会按当前周期与状态展示实体推荐。
- 底部时间轴支持按天 / 周 / 月浏览周期。

设置子视图：

- 修改播放列表名称、描述与关注标的
- 上传 / 清除背景图
- 事件地图数据维护区展示事件覆盖、状态计数和地图快照状态，并提供补齐事件抽取、全部重新抽取、停止当前抽取任务与构建地图；活跃任务继续展示月份和视频级进度
- 重新生成全部简报
- 修改简报聚合粒度
- 编辑播放列表专属简报提示词
- 删除播放列表

分析子视图：

- 页面功能名称为“事件语义星域”，不再提供“时间演化”或“变化点”tab。主视图默认是三维语义星域，X/Y/Z 都来自固定语义投影，时间不作为 Z 轴。
- 星点是可点选的 canonical 真实事件：单击只在原镜头下选中该点，以白芯金环和带引线的标题标签标明位置，并在右侧展开记录、实体、故事与证据详情；右侧“定位”才会移动镜头。直接点击主题标签会选中主题、打开右侧详情，并在当前窗口中保留成员的语义颜色、加亮和绘制细金边；其他事件降至约 48% 不透明度以维持全局语义形态。再次点击同一标签或关闭主题条件即可恢复。直接点击不会改变旋转、缩放或平移；只有搜索结果中的主题会主动聚焦对应语义区域。已选主题标签跨语义层级保留显示。
- 星域状态、覆盖计数、三维语义说明和时间/类型/镜头控制共用同一条响应式工具栏；宽屏保持单行，窄屏仅按内容换行，不再分为两条标题栏。
- 画布占满主体，支持左键旋转、滚轮或双指缩放、右键或修饰键平移、镜头复位和一键正交俯视。每个浏览器会话首次进入新快照时有约 4 秒短入场，用户操作或减少动态效果设置会立即取消。
- 主画面直接渲染当前窗口内的真实事件点，不再叠加大尺寸光晕、模糊星云或 bloom。点的局部疏密表达事件聚集程度；没有装饰性星点、旋臂、数字聚合圆圈、全图实体连线或全期参照。点云后方使用低透明度蓝紫色弯曲坐标网格提供三维空间参照：网格依据快照一级星域的中心与规模形成浅曲率，边缘渐隐、无刻度、无持续动画，不参与筛选、点选或时间窗口计算。
- 语义缩放分为一级星域、二级主题和真实事件近景。远景最多显示 20–28 个一级标签，中景最多 50–80 个二级标签，近景最多显示 24 个当前窗口主题代表事件标题；标签来自结构化实体、事件类型和中心代表事件，并执行屏幕碰撞消解与边缘避让。普通标签使用略微弱化的语义色文字、透明背景和暗色文字光晕，不显示常驻边框；悬停或键盘聚焦时恢复深色承托和语义色细描边，已选标签继续使用金色卡片强调。标签始终保持完全不透明。拖动时间轴时，标签和星图使用同一个预览窗口，取消拖动后恢复已提交窗口；标签宽度由浏览器实际排版结果测量，桌面端最长 460px，并随画布变窄而收缩。
- 搜索、六对象说明和当前窗口实体排行放入默认收起的左侧抽屉。右侧详情只在选中主题或事件后打开：一级星域列出当前窗口的二级主题，二级主题列出当前窗口最多 20 个代表事件，选中事件再展开记录、实体角色、证据和故事关系等结构化分区；不再围绕事件生成卫星节点。
- 当前窗口事件按八个稳定语义族着色：宏观与政策、市场与资产、公司与资本动作、产业与供给链、科技与网络安全、地缘/法律/监管、消费与社会、其他；底部图例解释颜色。金色表示选中事件，绿色表示实体筛选命中，紫色或青色短程曲线只表示有证据的故事关系。
- 地图提示优先展示事件标题、发生时间和类型；canonical UUID 只作为可复制的技术信息，不作为地图主 Tooltip。
- 底部只有一条基于 `event_time` 的月度时间轴。柱高表示覆盖该月的事件记录，轨道按可用宽度自适应显示月 / 年刻度；绿色窗口可整体拖动，点击任意月将其设为窗口结束月。正常 / 全量只决定可浏览的数据域，不是第二个时间范围。
- 地图只由一个固定长度观察窗口驱动，默认 12 个月，可切换 1/3/6/12 个月。播放按 1,000 ms/月整体移动窗口，到末尾自动停止；粒子 shader 直接依据每个事件的时间区间裁剪，提交窗口时在原坐标执行约 220 ms 交叉淡化。拖动时间轴时 GPU 实时预览对应窗口，不产生轨迹、残影或镜头漂移；播放期间不请求实体排行。
- 时间精度按区间解释：second/day 使用具体日期，month 覆盖整月，year 覆盖整年。页面不以 `available_at` 定位事件。
- 顶部区分 embedding 覆盖、未入地图和失败；地图构建期间可继续浏览 current ready snapshot。
- 分析子视图可见期间使用单飞的 compact manifest 轮询感知后台更新：构建或回填活跃时每 1.8 秒检查，idle 或请求错误时每 10 秒检查。浏览器页面隐藏后暂停 timer 并取消在途请求，重新可见时立即检查一次；切出子视图、切换播放列表或离开页面时清理 timer、请求和 `visibilitychange` 监听。
- compact 轮询只在 `snapshot_id` 变化或本地 scene 缺失时请求完整 manifest 与 scene；同一 snapshot 只合并构建状态，不重复下载场景。新 scene 在隐藏 mount 中完成初始化后再替换旧 controller，完整请求或初始化失败时保留旧地图，迟到响应会通过 playlist、subview 和 lifecycle generation 校验丢弃。
- 页面使用本地固定版本 Three.js 和 WebGL2；不支持 WebGL2 或 context 无法恢复时显示明确错误。静止时停止动画帧，正常像素比不超过 2，持续低帧率时降至 1.5，不删减事件或改变坐标。切出分析子视图时释放 renderer、材质、render target、observer 和事件监听。
- 播放列表作为同一内容来源集合，不按媒体来源切分或加权。来源权重是明确延后项，不在 UI 中伪装为可调参数。
- 抽取、全量重抽、停止任务和地图重建集中在设置子视图。

### 任务页

- 支持查看活动任务与已完成任务。
- 支持按类型、时间范围筛选。
- 支持实时刷新、查看错误详情、取消 / 重试任务。
- 包含任务统计图与实时任务快照。

### MCP Server 页面

- 说明 MCP Server 的连接地址、Bearer 鉴权、可用 tools / resources。
- 内置 VS Code / Codex CLI / OpenClaw 等接入示例。

### 设置页

当前标签页：

- `Cookies`
- `字幕`
- `会员`
- `画质`
- `LLM`
- `清理`
- `主屏幕`

各标签当前职责：

- `Cookies`：保存 YouTube / B 站 Netscape `cookies.txt`
- `字幕`：控制是否下载字幕 / 自动字幕
- `会员`：控制是否尝试下载 YouTube 会员专享视频
- `画质`：管理 `yt-dlp --format`
- `LLM`：编辑全局转写润色提示词
- `清理`：扫描并清理遗留 discovered 视频记录
- `主屏幕`：PWA 安装引导

## 当前交互特点

- 大多数写操作都会给出 toast，并可跳转到任务页查看异步执行结果。
- 任务页和概览页依赖 `/api/ws/jobs` 与 `/api/ws/job_stats` 做实时刷新。
- 概览页首屏依赖 `/api/stats`；其他页面启动时不等待 `/api/stats` 完成，统计信息允许延后静默刷新。
- 播放列表简报支持复制文本、打开原文、重新生成当前周期或全量重建。
- 设置页以运行时配置为主，保存后通常无需重启服务。

## 当前 UI 边界

- 还没有独立的“Briefs 列表页”；简报阅读主要放在播放列表详情页。
- UI 仍以单用户桌面 / 平板场景为主，移动端重点保障浏览、播放和 PWA 安装路径。
- MCP 页面是前端说明页，不等价于 MCP 实际 endpoint；真实接口由主 API 进程挂载在 `/mcp` 提供。

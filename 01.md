# raelyn（MVP）详细技术方案

基于 `yt-dlp + ffmpeg` 的单用户媒体订阅与视频采集系统：支持 YouTube / B站媒体同步、批量/增量下载、字幕与转写（可选）、播放列表聚合（先支持“添加媒体”）、按天生成 Markdown 简报（LLM：Ollama 或 OpenAI-compatible 在线推理）。

约束与决策（来自需求澄清）：
- 单用户（暂不做鉴权/审计）
- 媒体数量 < 1000
- MVP：不处理需要登录/付费内容
- “实时同步”目标：分钟级
- 简报产物：Markdown
- 播放列表：MVP 仅支持“添加媒体”，其余后续扩展
- 任务管理：参考 `job-mgmt-prompt.md` 的并发与可观测性原则（统一任务存储、Web/Worker 解耦、原子领取、幂等、回收与可观测性）

---

## 1. 基本概念（Domain Model）

### 1.1 Provider
- `provider`: `youtube` | `bilibili`
- `provider_media_id`: 平台侧频道/UP 主唯一 ID（不是名称）
- `provider_video_id`: 平台侧视频唯一 ID（YouTube video id / B站 BV/AV）

### 1.2 Media（媒体）
表示一个内容源（YouTube 频道 / B站 UP 主）：
- 可被“同步”以更新头像、简介、订阅量等元数据
- 可被“抓取/同步视频列表”以发现新视频

### 1.3 Video（视频）
表示一个可下载/可播放/可分析的视频条目：
- 只存元数据与处理状态；大文件内容不落地在 DB
- 产物统一通过 Asset 关联

### 1.4 Asset（产物/资源）
统一抽象：视频文件、音频文件、字幕文件、标准化字幕文本、ASR 转写文本、简报等都属于 Asset：
- 内容本体存 MinIO（S3）
- DB 存引用与索引（s3_key、类型、格式、语言、校验和等）

建议的 `asset.type`：
- `video`：下载后的视频文件（可能是 mp4/webm 等）
- `audio`：从视频分离的音频（建议 m4a 或 mp3，按实际转码策略）
- `subtitle`：原始字幕文件（vtt/srt/ass/...）
- `transcript`：标准化文本（建议同时产出 `json` 分段+时间戳 与 `txt` 纯文本）
- `brief`：按天简报 Markdown（按播放列表聚合）

### 1.5 Playlist（播放列表）
MVP：播放列表仅是“媒体集合”：
- `playlist`：列表本身
- `playlist_media`：列表包含哪些媒体

（后续扩展点：自动同步规则、已播放记录、自动播放最新等）

### 1.6 Job（任务）
所有重任务（同步媒体、拉取新视频、下载、转写、简报）都以 Job 表示：
- API 只负责“创建/查询/运维”，不在请求线程执行重活
- Worker 通过 DB 原子领取任务执行
- 任务可拆分父子任务（批量下载=父任务，单视频下载=子任务）

---

## 2. 总体架构与数据流

### 2.1 组件划分
- `api`（FastAPI 推荐）：提供 HTTP API + 静态 UI（/static）+ 任务创建/查询
- `worker`：执行下载/处理/转写/总结等任务（可多进程/多实例）
- `scheduler`（可与 api 进程合并为定时协程/独立进程）：分钟级触发“同步媒体新视频”的任务投递
- 外部依赖：
  - `yt-dlp`（default + curl_cffi）抓取与下载
  - `ffmpeg` 分离音频/转码
  - `PostgreSQL`（任务表/元数据）
  - `MinIO`（对象存储）
  - `qwen3-asr`（可选 ASR）
  - `LLM`（简报/笔记；可对接 Ollama 或在线推理）

### 2.2 关键数据流（MVP）
1) 添加媒体
- UI -> API 创建 media
- API 投递 `media.sync_profile`（同步头像/描述等）与 `media.sync_videos`（发现视频列表）任务

2) 分钟级增量同步
- scheduler 每 N 分钟：为需要同步的媒体投递 `media.sync_videos`（幂等：同时间窗/同 cursor 不重复）
- 发现新视频后为每个视频创建 `video` 记录（upsert），并可按配置自动投递下载任务

3) 下载与处理
- `video.download`：使用 yt-dlp 下载视频/字幕到临时目录
- `video.extract_audio`：ffmpeg 分离音频并上传 S3
- `video.normalize_subtitle`：字幕转标准文本（S3）
- 若无中文字幕且配置了 ASR：投递 `video.asr_transcribe`

4) 简报（按天）
- `brief.generate_daily`：按播放列表+日期聚合当日已就绪的 transcript（或字幕文本），调用 LLM 生成 Markdown，上传 S3 并落库

---

## 3. 任务管理（Job Mgmt）设计

### 3.1 任务状态机（建议）
- `pending`：待执行
- `running`：执行中（带租约 lease）
- `succeeded`：成功
- `failed`：失败（可重试或终止）
- `canceled`：人工取消

扩展字段：
- `attempt` / `max_attempts`
- `scheduled_for`：定时执行（分钟级同步靠它）
- `lease_expires_at`：running 的租约到期时间（防卡死回收）
- `progress_current` / `progress_total`

### 3.2 原子领取（Atomic Claim）
Worker 领取任务必须是 DB 原子更新（避免重复执行），推荐模式：
- 查询并锁定：`SELECT ... FOR UPDATE SKIP LOCKED`
- 条件更新：`pending -> running` 且 `scheduled_for <= now()` 且（`lease_expires_at is null OR lease_expires_at < now()`）

领取成功后写入：
- `started_at`
- `worker_id`
- `lease_expires_at = now() + lease_duration`

### 3.3 租约/心跳与回收
- worker 执行长任务时周期性刷新 `lease_expires_at`
- scheduler/worker 启动时可执行“回收扫描”：
  - `status=running AND lease_expires_at < now()` 视为失联，转 `pending` 或 `failed`（记录回收原因）

### 3.4 幂等策略（At-least-once 友好）
- 数据写入侧用唯一键+upsert 保证重复任务不会重复产出：
  - `videos(provider, provider_video_id)` 唯一
  - `assets(video_id, type, format, language, source, variant)` 视业务定义唯一（至少避免同一产物重复）
- Job 层设置 `idempotency_key`（可选，但建议）：同一任务参数签名（例如 `media.sync_videos:{media_id}:{window}`）

### 3.5 并发与外部依赖门控（Guardrails）
外部依赖很容易触发风控/资源打架，MVP 建议：
- Provider 级并发：YouTube 同时下载数（例如 2），B站（例如 2）
- Media 级并发：同一媒体同一时刻只允许 1 个同步/下载任务（避免重复拉取）

实现方式（推荐从易到难）：
1) 进程内信号量（单 worker 实例）+ DB 约束（跨实例需要更多手段）
2) PostgreSQL advisory lock（跨 worker 实例简单有效）：
   - `pg_try_advisory_lock(hashtext('youtube:download'))` 控制 provider 并发
   - `pg_try_advisory_lock(hashtext('media:{media_id}:sync'))` 控制媒体并发

### 3.6 可观测性（Visibility by Design）
- 每个 job 记录：`type/status/params/result/error/attempt/worker_id`
- 结构化日志：每条日志带 `job_id` 与 `video_id/media_id`
- `job_events` 存关键事件（可选但强烈建议，便于 UI 展示与排障）

---

## 4. 数据库定义（PostgreSQL）

说明：
- 使用 `uuid` 作为主键（或 `bigserial` 亦可）
- 时间统一 `timestamptz`
- JSON 使用 `jsonb`

### 4.1 DDL（建议草案）

```sql
-- Providers: 'youtube' | 'bilibili'
create table media (
  id uuid primary key,
  provider text not null,
  provider_media_id text not null,
  url text not null,
  name text,
  avatar_url text,
  description text,
  subscriber_count bigint,
  video_count bigint,
  sync_cursor jsonb,                 -- 增量同步游标（按 provider 定义）
  last_profile_sync_at timestamptz,
  last_video_sync_at timestamptz,
  created_at timestamptz not null,
  updated_at timestamptz not null,
  unique (provider, provider_media_id)
);
create index media_provider_idx on media(provider);

create table video (
  id uuid primary key,
  provider text not null,
  provider_video_id text not null,
  media_id uuid not null references media(id) on delete cascade,
  url text not null,
  title text,
  description text,
  thumbnail_url text,
  published_at timestamptz,
  duration_sec integer,
  view_count bigint,
  like_count bigint,
  comment_count bigint,
  tags text[],
  raw_info jsonb,                    -- yt-dlp info.json 的裁剪/存档（避免太大）
  status text not null,              -- discovered|downloading|ready|failed
  error_message text,
  created_at timestamptz not null,
  updated_at timestamptz not null,
  unique (provider, provider_video_id)
);
create index video_media_published_idx on video(media_id, published_at desc);
create index video_status_idx on video(status);

create table asset (
  id uuid primary key,
  video_id uuid references video(id) on delete cascade,
  type text not null,                -- video|audio|subtitle|transcript|brief
  format text not null,              -- mp4|webm|m4a|vtt|srt|json|txt|md...
  language text,                     -- zh|en|...
  source text not null,              -- ytdlp|subtitle_auto|subtitle_manual|qwen3-asr|ollama|llm...
  variant text,                      -- 可选：例如 'raw'|'normalized'|'segments'
  s3_bucket text not null,
  s3_key text not null,
  size_bytes bigint,
  checksum_sha256 text,
  metadata jsonb,
  created_at timestamptz not null,
  unique (video_id, type, format, coalesce(language, ''), source, coalesce(variant, ''))
);
create index asset_video_type_idx on asset(video_id, type);

create table playlist (
  id uuid primary key,
  name text not null,
  description text,
  created_at timestamptz not null,
  updated_at timestamptz not null
);
create unique index playlist_name_ux on playlist(lower(name));

create table playlist_media (
  playlist_id uuid not null references playlist(id) on delete cascade,
  media_id uuid not null references media(id) on delete cascade,
  added_at timestamptz not null,
  primary key (playlist_id, media_id)
);
create index playlist_media_media_idx on playlist_media(media_id);

create table daily_brief (
  id uuid primary key,
  playlist_id uuid not null references playlist(id) on delete cascade,
  brief_date date not null,          -- 以服务器配置时区为准
  status text not null,              -- pending|running|ready|failed
  markdown_asset_id uuid references asset(id),
  error_message text,
  created_at timestamptz not null,
  updated_at timestamptz not null,
  unique (playlist_id, brief_date)
);

create table job (
  id uuid primary key,
  type text not null,                -- media.sync_profile|media.sync_videos|video.download|...
  status text not null,              -- pending|running|succeeded|failed|canceled
  priority integer not null default 0,
  params jsonb not null,
  result jsonb,
  progress_current integer,
  progress_total integer,
  error_message text,
  error_stack text,
  attempt integer not null default 0,
  max_attempts integer not null default 5,
  scheduled_for timestamptz not null default now(),
  created_at timestamptz not null,
  started_at timestamptz,
  finished_at timestamptz,
  lease_expires_at timestamptz,
  worker_id text,
  parent_job_id uuid references job(id) on delete set null
);
create index job_claim_idx on job(status, scheduled_for, priority desc);
create index job_lease_idx on job(status, lease_expires_at);
create index job_type_idx on job(type);

create table job_event (
  id bigserial primary key,
  job_id uuid not null references job(id) on delete cascade,
  ts timestamptz not null,
  level text not null,               -- info|warn|error
  message text not null,
  data jsonb
);
create index job_event_job_ts_idx on job_event(job_id, ts desc);

create table app_config (
  key text primary key,
  value jsonb not null,
  updated_at timestamptz not null
);
```

### 4.2 关键索引与约束说明
- `videos(provider, provider_video_id)` 唯一：发现视频幂等
- `assets(...)` 唯一：产物写入幂等（避免重复上传）
- `daily_brief(playlist_id, brief_date)` 唯一：一天一份简报（可重跑覆盖/更新）
- `job_claim_idx`：高频领取路径索引

---

## 5. 对象存储（MinIO / S3）布局

### 5.1 Bucket
建议统一一个 bucket（例如 `raelyn`），便于管理生命周期；也可按环境拆分。

### 5.2 Key 命名规范（建议）
```
{provider}/{provider_media_id}/{provider_video_id}/
  video/{variant}.{ext}
  audio/{variant}.{ext}
  subtitle/{lang}/{variant}.{ext}
  transcript/{lang}/{variant}.{ext}        # txt/json
  brief/{playlist_id}/{YYYY-MM-DD}.md
  meta/info.json
```

### 5.3 文件写入策略
- 统一先写临时 key（或本地临时文件）-> 校验 -> 再写最终 key
- 记录 `checksum_sha256`（可选但推荐）用于去重与校验

---

## 6. 主要功能模块定义（Backend）

### 6.1 媒体管理（Media）
能力：
- 添加/删除媒体
- 同步媒体资料（头像、名称、描述、订阅数等）
- 同步媒体视频列表（增量发现新视频）

建议的同步实现（MVP 可先粗后精）：
- YouTube：优先用 yt-dlp extractor 获取 channel/playlist 的 flat 列表 + 再按视频补全
- B站：同理，按 UP 主视频列表增量抓取
- `sync_cursor` 保存“已同步到的最新发布时间/页码/last_video_id”等（按 provider 具体策略）

输出：
- 新视频落库为 `video.status=discovered`
- 可配置是否自动投递下载任务（MVP 建议默认开）

### 6.2 视频下载（yt-dlp）
`video.download` 任务：
- 输入：`video_id`
- 行为：
  - 生成临时工作目录（按 job_id）
  - yt-dlp 下载视频文件、字幕（若有）、缩略图、info.json
  - 将 video 文件作为 `asset(type=video)` 上传
  - 写入/更新 `video.status`（downloading -> ready 或 failed）

注意点：
- 失败原因要落到 `job.error_message/error_stack` 与 `video.error_message`
- 下载可用“断点续传/重试”，但写入产物必须幂等（避免重复上传）

### 6.3 音频分离（ffmpeg）
`video.extract_audio` 任务：
- 输入：`video_id` + `video_asset_id`
- 输出：`asset(type=audio)`

策略：
- 优先无损抽取（copy）或最小转码（视容器/编码情况决定）
- 音频用于 ASR/分析，体积与质量权衡可配置

### 6.4 字幕处理（Normalize Subtitle -> Transcript）
`video.normalize_subtitle` 任务：
- 输入：字幕 asset（vtt/srt/ass），优先选择中文（`zh/zh-Hans/zh-CN`）
- 输出：
  - `asset(type=transcript, format=json, variant=segments)`：分段 + 时间戳
  - `asset(type=transcript, format=txt, variant=plain)`：纯文本

规则建议：
- 保留时间戳（对齐播放/定位）
- 合并过碎片段，过滤纯标点/噪声

### 6.5 ASR（qwen3-asr，可选）
触发条件：
- 未检测到中文字幕 transcript，且配置了 ASR 服务地址

`video.asr_transcribe` 任务：
- 输入：audio asset + 期望语言（可选）
- 输出：
  - `asset(type=transcript, source=qwen3-asr, format=json/txt)`

接口抽象建议（便于替换供应商）：
- `POST /v1/audio/transcriptions`：传入音频（multipart file），返回 segments + text（OpenAI-compatible）

### 6.6 按天简报（LLM -> Markdown）
`brief.generate_daily` 任务：
- 输入：`playlist_id` + `date`（本地日历日）+ `timezone`
- 数据选择：
  - 取播放列表包含的媒体当日发布的视频
  - 只选择“已就绪”的 transcript（优先中文字幕，其次 ASR）
  - 对超长文本做切块（chunk），保留来源引用（video_id/title/link）
- 调用 LLM 生成 Markdown：
  - 标题、要点、主题分类、重要引用（可选）、建议行动项（可选）
- 输出：
  - `asset(type=brief, format=md, source=llm)` 上传
  - `daily_brief.status=ready` 关联 markdown_asset_id

---

## 7. API 接口定义（FastAPI，REST）

约定：
- JSON：`camelCase` 或 `snake_case` 任选其一，但全局一致（建议 snake_case 贴近 Python）
- 统一返回：`{ "data": ..., "error": null }` 或直接资源 JSON（MVP 可简单）
- 所有列表接口支持分页：`limit`/`offset` 或 `cursor`
- 时间字段用 ISO8601

### 7.1 Health
- `GET /api/health`
  - 返回：服务状态、DB/S3 连接可用性（可选）

### 7.2 Media
- `POST /api/media`
  - body：`{ "provider": "youtube", "url": "..." }`
  - 行为：解析 url -> 提取 provider_media_id -> upsert media
  - side effects：投递 `media.sync_profile` + `media.sync_videos`
- `GET /api/media?provider=&q=&limit=&offset=`
- `GET /api/media/{media_id}`
- `DELETE /api/media/{media_id}`
  - 行为：删除媒体及其视频（cascade）或仅取消订阅（单用户下等价）
- `POST /api/media/{media_id}/sync`
  - 行为：投递一次 `media.sync_profile` + `media.sync_videos`

### 7.3 Videos
- `GET /api/videos?provider=&media_id=&status=&from=&to=&limit=&offset=`
  - 支持按时间/媒体筛选（满足需求 8）
- `GET /api/videos/{video_id}`
- `POST /api/videos/{video_id}/download`
  - 行为：投递 `video.download`（以及后续 extract/normalize/asr 的编排）

### 7.4 Assets / Content
目标：支持 HTTP 获取视频/音频/文字内容（需求 9）。

推荐做法（MinIO 可直连时）：
- `GET /api/assets/{asset_id}`
  - 返回 asset 元信息 + `presigned_url`（短时有效）

若 MinIO 不对外暴露，则 API 代理：
- `GET /api/assets/{asset_id}/content`
  - 支持 `Range`（视频播放必需），返回流式响应

### 7.5 Jobs（任务管理）
- `GET /api/jobs?status=&type=&limit=&offset=`
- `GET /api/jobs/{job_id}`
- `POST /api/jobs/{job_id}/cancel`
- `POST /api/jobs/{job_id}/retry`
- `GET /api/jobs/{job_id}/events`

### 7.6 Playlists（MVP：仅媒体集合）
- `POST /api/playlists`
  - body：`{ "name": "...", "description": "..." }`
- `GET /api/playlists`
- `GET /api/playlists/{playlist_id}`
- `DELETE /api/playlists/{playlist_id}`
- `POST /api/playlists/{playlist_id}/media`
  - body：`{ "media_id": "..." }`
- `DELETE /api/playlists/{playlist_id}/media/{media_id}`

### 7.7 Briefs（简报）
- `POST /api/briefs/generate`
  - body：`{ "playlist_id": "...", "date": "YYYY-MM-DD" }`
  - 行为：投递 `brief.generate_daily`
- `GET /api/briefs?playlist_id=&from=&to=`
- `GET /api/briefs/{brief_id}`
  - 返回：元信息 + markdown asset 获取方式（presigned 或 content）

### 7.8 Settings（运行配置）
- `GET /api/config`
- `PUT /api/config`
- 保存到 `app_config`（例如 asr_url、llm_url、并发、同步间隔、默认自动下载开关等）

---

## 8. 前端界面设计（参考 ui-framework-prompt.md）

约束（来自 `ui-framework-prompt.md`）：
- 技术栈：TailwindCSS + Alpine.js
- 禁止 CDN，静态资源从 `/static/` 加载
- 应用壳：顶部状态栏 + 左侧菜单 + 主体内容区
- 左侧菜单切换视图：`activeView` + `x-show`/`x-transition`

### 8.1 全局布局（App Shell）
顶部状态栏：
- 左：当前视图标题（Media/Videos/Jobs/Playlists/Briefs/Settings）
- 中：全局状态（例如：最近一次同步时间、队列 pending 数、worker 在线/离线）
- 右：快速操作（“添加媒体”、“立即同步全部”、“生成今日简报”）

左侧菜单（至少 3 个一级项，建议 6 个）：
- 概览（Overview）
- 媒体（Media）
- 视频（Videos）
- 任务（Jobs）
- 播放列表（Playlists）
- 简报（Briefs）
- 设置（Settings）

### 8.2 概览页（Overview）
卡片组件（rounded-2xl + shadow）：
- 今日新增视频数
- pending/running job 数
- 最近失败任务数（点击跳转 Jobs 并过滤 failed）
- 存储占用（可选：从 S3 统计或估算）

### 8.3 媒体页（Media）
功能：
- 媒体列表：头像、名称、provider、订阅数、最近同步时间
- 搜索框（按名称/描述模糊）
- 操作：添加媒体、删除、立即同步
- 点击进入媒体详情（可选 MVP）：展示该媒体视频列表（最近 N 条）与“批量下载全部（父任务）”

交互：
- 添加媒体弹窗：provider + URL
- 同步按钮：触发 `/api/media/{id}/sync` 并在顶部状态栏提示“已投递任务”

### 8.4 视频页（Videos）
功能：
- 筛选：时间范围、媒体、状态（discovered/downloading/ready/failed）
- 列表列：标题、媒体、发布时间、时长、状态、产物（video/audio/transcript 图标）
- 详情抽屉/弹窗（MVP 可选）：展示可播放链接（video/audio）、字幕/文本下载

### 8.5 任务页（Jobs）
功能：
- 列表：type、status、scheduled_for、started/finished、attempt、progress、error
- 过滤：status/type
- 操作：retry/cancel
- 详情：展示 params/result、事件日志（job_events）

### 8.6 播放列表页（Playlists，MVP）
功能：
- 创建/删除播放列表
- 在播放列表中添加/移除媒体（多选弹窗）
- 展示播放列表包含媒体数量、最近简报日期

### 8.7 简报页（Briefs）
功能：
- 选择播放列表 + 日期范围
- 列表：日期、状态、生成时间
- 查看简报：Markdown 渲染（Alpine + 简单 markdown 渲染库可选；或后端返回已渲染 HTML）
- 操作：生成指定日期简报

### 8.8 设置页（Settings）
配置项（对应 `app_config`）：
- 同步间隔（分钟）
- 自动下载开关（发现新视频是否自动投递下载）
- 下载并发（按 provider）
- ASR 服务地址（可空）
- LLM 推理 endpoint URL（可空）
- 存储 bucket / 前缀（可选）

---

## 9. MVP 里程碑（建议）

1) 基础设施与数据模型
- Postgres + MinIO 连接
- media/video/asset/job 表与基础 CRUD

2) 任务系统（最小可用）
- job 创建/查询/领取/租约/重试
- worker 跑通一个简单任务（例如 sleep + progress）

3) 媒体同步（发现视频）
- 添加媒体 -> `media.sync_videos` -> upsert videos

4) 下载与产物链路
- `video.download` -> `asset(video)`
- `video.extract_audio` -> `asset(audio)`
- `video.normalize_subtitle` -> `asset(transcript)`
- 视频列表可筛选、可获取内容（presigned 或代理）

5) 播放列表（媒体集合）+ 简报（Markdown）
- playlist CRUD + playlist_media
- `brief.generate_daily` + brief 列表/查看

6) UI（App Shell + 核心页面）
- Media / Videos / Jobs / Playlists / Briefs / Settings

---

## 10. 配置项清单（建议）

环境变量（示例）：
- `DATABASE_URL`
- `S3_ENDPOINT` / `S3_ACCESS_KEY` / `S3_SECRET_KEY` / `S3_BUCKET` / `S3_REGION`
- `S3_USE_SSL`（true/false）
- `FFMPEG_PATH`（可选）
- `SYNC_INTERVAL_MINUTES`（默认 5）
- `YOUTUBE_DOWNLOAD_CONCURRENCY` / `BILIBILI_DOWNLOAD_CONCURRENCY`
- `ASR_URL`（可空）
- `LLM_URL`（可空）
- `TIMEZONE`（默认 `Asia/Shanghai` 或由部署决定）

---

## 11. 风险与处理（MVP 关注点）
- 平台风控/限速：必须内建 provider 并发门控 + 重试退避
- 大文件与播放：若走 API 代理，务必支持 `Range`；否则视频播放体验差
- 字幕/语言多样性：语言检测与优先级策略要可配置（优先中文）
- 文本过长导致 LLM 输入超限：简报生成必须做 chunking 与摘要合并
- 存储膨胀：后续可加生命周期（只保留音频/文本，或只保留最近 N 天视频）

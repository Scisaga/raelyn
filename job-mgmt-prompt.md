# 任务并发与可观测性设计原则

## 1) 单一事实源（Single Source of Truth）
- 用一个统一的“任务存储”（任务表/队列/消息系统）承载任务的**生命周期状态**与**执行结果**；不要把真实执行状态只藏在进程内存里。
- 任务记录至少应包含：`type/name`、`status`、`params`、`created_at/started_at/finished_at`、`progress`、`error/stack`、`worker_id`（可选）。

## 2) Web/API 与执行面解耦
- Web/API 进程只做：创建任务、查询任务、有限的运维操作；**不在请求线程内跑重活**，避免超时、阻塞与不可控资源占用。

## 3) 并发优先“多进程/多 worker + 任务拆分”，避免任务内部嵌套并发
- 并发的主要来源是**多个独立 worker**（进程或容器实例）。
- 大任务通过**拆分为多个子任务**表达并行度，而不是在单个任务函数里再开线程池/进程池（减少不可观测与资源打架）。

## 4) 原子领取（Atomic Claim / Compare-and-Set）
- worker 领取任务必须是**原子操作**（例如条件更新/抢占锁）：只允许一个 worker 把 `pending -> running` 成功，避免重复执行。
- “到期任务”和“普通任务”应有清晰的领取策略，避免未来任务阻塞当前任务（例如按 `scheduled_for` 过滤）。

## 5) 外部依赖门控（Concurrency/Rate Guardrails）
- 把外部系统的**并发上限、连接数上限、速率限制、风控策略**内建为明确的门控（信号量/令牌桶/互斥锁/分布式锁）。
- 默认配置以稳定可复现为优先；提升并发必须显式配置，而不是隐式放开。

## 6) 资源生命周期最小化（尤其是 DB 连接）
- 任务执行前后再打开/关闭关键资源；避免“长任务持有连接/事务”导致连接池耗尽。
- 将“读取任务元数据”和“执行重活”在资源占用上解耦（重活期间不占用不必要资源）。

## 7) 幂等与可重跑（At-least-once 友好）
- 默认假设任务可能被重试、重复投递或执行中断：写入侧应尽量**幂等**（upsert/唯一键/幂等键/版本号）。
- 明确幂等锚点（例如时间点/窗口/批次号/任务参数签名），并确保“重复跑得到相同结果”。

## 8) 进度模型与任务编排（Parent-Child）
- 对可拆分的任务，用父子任务建模：父任务只汇总进度与状态，子任务承载实际计算。
- 父任务进度聚合要可解释（完成数、失败数、运行中数、总数），便于 UI/排障使用。

## 9) 故障恢复与状态回收
- 任务失败要**显式记录失败原因**（含可定位的错误信息/栈），不要吞错。
- 对“running 卡死”要有回收机制：超时/心跳/租约（lease）到期后回到可重试状态，并记录回收原因。
- 重试策略要可控：哪些错误可重试、间隔、最大次数、最终去向（例如 dead-letter）。

## 10) 可观测性（Visibility by Design）
- 结构化日志：每条任务日志必须包含 `job_id/type/status/attempt/worker_id` 等关联字段，便于聚合检索。
- 指标（metrics）：吞吐（创建/完成）、延迟（queue latency、run duration）、失败率、重试次数、外部依赖调用统计。
- 时间维度视图：按时间桶聚合 pending/running/done 的数量与类型分布，用于快速发现洪峰与异常集中。
- 安全与脱敏：日志/错误信息默认不包含密钥、token、密码、完整请求体等敏感数据。